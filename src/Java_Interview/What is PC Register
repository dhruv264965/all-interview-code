“The Program Counter (PC Register) is a special register in the CPU that stores the address of the next instruction to
 execute. After executing each instruction, the PC is incremented automatically, unless a jump/branch changes it.
  In JVM, each thread has its own PC register to track execution.”